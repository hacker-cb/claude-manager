# Screenshots

Image assets referenced from the root [README](../../README.md).

**Wanted (tracked as a follow-up):**

- `main-window.png` — the management window showing a few profiles with distinct
  badge colors and labels.
- `menu-bar.png` — the menu bar extra with its quick open/stop actions.

Capture on a Retina display, crop to the window, and keep the file reasonably small.
Once added, uncomment the `![…](docs/images/…)` lines near the top of the root README.
